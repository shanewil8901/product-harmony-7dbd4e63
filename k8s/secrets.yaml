# TEMPLATE ONLY — do not commit real values.
# Replace with a SealedSecret / External Secret in production.
apiVersion: v1
kind: Secret
metadata:
  name: erp-secrets
  labels:
    app.kubernetes.io/part-of: erp
type: Opaque
stringData:
  DB_USERNAME: "root"
  DB_PASSWORD: "change-me"
  MYSQL_ROOT_PASSWORD: "change-me"
  JWT_SECRET: "change-me-in-production"
  REDIS_PASSWORD: ""

apiVersion: v1
kind: Service
metadata:
  name: erp-frontend
  labels:
    app: erp-frontend
spec:
  type: ClusterIP
  selector:
    app: erp-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
